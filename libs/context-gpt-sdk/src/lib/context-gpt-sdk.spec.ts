/**
 * Tests for the ContextGptSdk class are located in the context-gpt-backend-e2e project.
 * Instead of running separate API tests and SDK tests, we run them together are the SDK directly uses the API.
 */
